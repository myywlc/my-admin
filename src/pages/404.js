import React, { PureComponent, Fragment } from 'react';

export default class NoPath extends PureComponent {
  render() {
    return (
      <Fragment>
        404 no find the path
      </Fragment>
    );
  }
}
